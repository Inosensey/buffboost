# docker-compose.dev.yml
version: '3.8'

services:

  backend:
    build:
      context: .
      dockerfile: packages/backend/Dockerfile.dev 
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=development
      - DATABASE_URL=postgresql://neondb_owner:npg_L9cTwHXlMGP1@ep-flat-waterfall-a15odvz7-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
      - JWT_SECRET=pS|Yd$caqXP/]P@>P4r}733mV@P)dJ=HhqHYE%G@@Fd
      - STRIPE_SECRET_KEY=sk_test_51SxGw2AbPDgDak9i1O06vdiP21DJk3p1OjqiZUENoGTBIaPzbLPB4iFYRFKNJ0jSG3TKClSb0Py4OHYjzsGGYqe900caT2Neu6
    volumes:
      - ./packages/backend:/app/packages/backend
      - /app/node_modules
      - /app/packages/backend/node_modules
    command: npm run dev:backend

  frontend:
    build:
      context: .
      dockerfile: packages/frontend/Dockerfile.dev
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
      - NEXT_PUBLIC_API_URL=http://localhost:3001
      # Critical for Docker file watching:
      - WATCHPACK_POLLING=true
      - CHOKIDAR_USEPOLLING=true
      - CHOKIDAR_INTERVAL=1000
      # Turbopack specific:
      - TURBOPACK_POLLING=true
    volumes:
      - ./packages/frontend:/app/packages/frontend
      - /app/node_modules
      - /app/packages/frontend/node_modules
    command: npm run dev:frontend